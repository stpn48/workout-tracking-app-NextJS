/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com", "lh3.googleusercontent.com"], // Add more domains if necessary (e.g., Google OAuth)
  },
};

export default nextConfig;
